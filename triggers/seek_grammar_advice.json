{
    "type": "shortcut",
    "name": "Seek grammar advice",
    "description": "Trigger a request to seek grammar advice for text",
    "workflow": "#/workflows/grammar_advisor",
    "shortcut": {},
    "inputs": {
        "interactivity": {
            "value": "{{data.interactivity}}"
        },
        "channel_id": {
            "value": "{{data.channel_id}}"
        }
    }
}
